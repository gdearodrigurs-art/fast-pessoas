# dependências e build
node_modules/
.next/
out/
dist/

# ambiente e segredos — NUNCA commitar
.env
.env.*
!.env.example
*.pem
*.pfx

# artefatos locais
*.log
.DS_Store
Thumbs.db

# arquivos temporários do Office
~$*

# worktrees temporários de agentes
.claude/worktrees/
fast-pessoas/.claude/worktrees/

# conexao do banco local de desenvolvimento
.env.local-db
fast-pessoas/.env.local-db

# temporarios de agente
.tmp-*/
.tmp-*.js
fast-pessoas/.tmp-*/
_verif-*
fast-pessoas/_verif-*

# Saída da compilação da suíte (tsconfig.testes.json) — regerada a cada npm test.
fast-pessoas/.tmp-testes/
